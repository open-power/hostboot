# OCC artifact ID used to assemble PNOR image
# OCC is not automatically pulled in from github for standalone model.
# Need to create artifact directory (use commit number) in:
#     /gsa/ausgsa/projects/h/hostboot/simbuild/dependencies/occ/
# and then build 81e00430.lid and copy to that new directory
export OCC_ARTIFACT_ID=rchgsahack
