config FORCE_SINGLE_CHIP
    default n
    help
        Force FSI and MVPD presence detect to remote processor chips to return
        false, causing Hostboot only to detect a single processor chip (master).

config COMPILE_VPD_ECC_ALGORITHMS
    default n
    help
        Compile the VPD ECC algorithms that do the validation
        and updating of ECC data

